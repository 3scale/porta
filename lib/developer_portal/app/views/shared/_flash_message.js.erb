<%# TODO: Replace this partial with ThreeScale.flash or something  %>
<% if flash.now[:error] %>
   $.flash.error('<%= escape_javascript(flash.now[:error]) %>');
<% else %>
   $.flash.notice('<%= escape_javascript(flash.now[:notice]) %>');
<% end %>
