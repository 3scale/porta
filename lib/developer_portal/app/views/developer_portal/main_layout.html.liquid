<!DOCTYPE html>
<html lang="en">
  <head>
    <title>
      {% if page.title %}
        {{ page.title }}
      {% else %}
        {{ provider.name }}
      {% endif %}
    </title>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    {% csrf %}
    {% cdn_asset /bootstrap/3.0.0/bootstrap.css %}
    {% cdn_asset /font-awesome/4.3.0/css/font-awesome.css %}

    {% cdn_asset /jquery/3.5.0/jquery.min.js %}
    {% cdn_asset /bootstrap/3.0.0/bootstrap.min.js %}
    {% cdn_asset /jquery/jquery.colorbox-patched.js %}
    {% cdn_asset /3scale_v2.js %}
    {% cdn_asset /excanvas.compiled.js %}

    {{ content_of.stylesheets | html_safe }}
    {% cdn_asset /default.css %}

    {{ content_of.javascripts | html_safe }}
  </head>

  <body>
    <header role="banner">
      <nav class="navbar navbar-fixed-top navbar-inverse" role="navigation">
        <div class="container tabbed">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-1">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">{{  provider.name }}</a>
          </div>
          {% include 'submenu'%}
        </div>
      </nav>

      {% comment %}
        WARNING: Please note that if you remove the block below
        you developer portal will stop displaying errors and
        information messages such as 'Invalid password' on
        login form or similar.
      {% endcomment %}

        <div id="flash-messages">
          {% for message in flash.messages %}
            <div class="navbar navbar-fixed-top navbar-default alert alert-{{ message.type }}" data-dismiss="alert">
              <div class="container">
                  <button type="button" class="close"  aria-hidden="true">&times;</button>
                  {{ message.text }}
              </div>
            </div>
          {% endfor %}
        </div>
    </header>

    <main id="main-content" role="main">
      {% if request.path == '/' %}
        {% content %}
      {% else %}
        <div class="full">
          <div class="container">
            {% content %}
          </div>
        </div>
      {% endif %}
    </main>

    <footer role="contentinfo">
      <div class="container">
        <ul class="list-inline pull-right">
          <li>&copy; {{ today.year }} {{ provider.name }},</li>
          <li><a class="powered-by" href="http://3scale.net/">Powered by 3scale</a></li>
        </ul>
      </div>
    </footer>

    <!-- JS and analytics only. -->
    {% include 'analytics' %}
  </body>
</html>
