$("a[href='/finance/accounts/<%=@buyer.id%>/invoices/<%=@invoice.id%>/line_items/new']").hide(); // Hide the 'Add line items'
ThreeScale.toast('<%=j @line_item.errors.full_messages.to_sentence%>', 'danger');
$('form[action^="/finance/accounts/<%=@buyer.id%>/invoices/<%=@invoice.id%>/line_items/"]').each(function(){ // Destroy all the 'Dele line item' forms
    this.parentElement.removeChild(this);
});
$.colorbox.close();
