jQuery.colorbox.close();
var html = "<%= escape_javascript(render :partial => 'usage_limit', :locals => {:usage_limit => @usage_limit}) %>",
    id   = "#" + "<%= dom_id(@usage_limit) %>";

jQuery(id).replaceWith(html);
