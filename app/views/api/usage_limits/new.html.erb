<h2>New Usage limit</h2>

<%= semantic_form_for @usage_limit,
                      :url => polymorphic_path([:admin, @plan, @metric, :usage_limits]),
                      :remote => true do |form| %>
  <%= render 'form', :form => form %>
<% end %>
