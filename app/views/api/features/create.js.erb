jQuery.colorbox.close();

document.querySelector('table#features tbody').innerHTML += '<%= escape_javascript(render partial: 'feature', locals: { feature: @feature }) %>';

jQuery.flash.notice('Feature has been created.');
