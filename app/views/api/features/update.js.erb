jQuery.colorbox.close();
var html = "<%= escape_javascript(render :partial => 'feature', :locals => {:feature => @feature}) %>",
    id   = "#<%= dom_id(@feature) %>";
jQuery(id).replaceWith(html);
ThreeScale.toast('Feature has been updated.', 'success');
