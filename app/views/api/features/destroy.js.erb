<% if flash[:success].present? %>
  <%# If EventTarget is replaced immediately, ajax:success and ajax:complete are not called and the spinner will stay %>
  setTimeout(() => {
    document.getElementById('<%= dom_id(@feature) %>').remove();
  });
<% end %>

<%= render 'shared/flash_alerts' %>
