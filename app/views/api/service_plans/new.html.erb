<% content_for :page_header_title, "Create new Service Plan of #{@service.name}" %>

<%= semantic_form_for @plan, :url => admin_service_service_plans_path(@service) do |form| %>
  <%= render :partial => 'form', :object => form %>
  <%= form.actions do %>
    <%= form.commit_button %>
  <% end %>
<%- end %>
