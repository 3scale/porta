<% Rails.application.deprecators[:threescale].warn "Plans are being migrated to React this logic should be handled inside a component" %>
$.colorbox({ html: <%= json render('form', :plan => @plan) %> });
