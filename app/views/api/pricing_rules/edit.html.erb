<h2>Edit Pricing rule</h2>

<% @value = @pricing_rule.min %>

<%= semantic_form_for @pricing_rule,
                      :url => polymorphic_path([:admin, @plan, @pricing_rule]),
                      :remote => true do |form| %>
  <%= render 'form', :form => form %>
<% end %>
