<% alert_types = %w[default info warning success danger] %>

<% flash.each do |type, messages| %>
  <% if alert_types.include?(type) %>
    <% Array(messages).each do |message| %>
      ThreeScale.toast('<%= escape_javascript(message) %>', '<%= type %>');
    <% end %>
  <% end %>
<% end %>
