<% accounts.each do |account| %>
  document.querySelector('#<%= dom_id(account) %> td[data-label="State"]')
    .innerText = '<%= account.state.capitalize %>'
<% end %>
$.colorbox.close()
<%= render 'shared/flash_alerts' %>
