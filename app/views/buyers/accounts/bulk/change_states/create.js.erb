<%- @accounts.each do |account| %>
  $("#<%=dom_id(account)%>").find(".state").text("<%= escape_javascript account.state.capitalize %>");
<%-end-%>
$.colorbox.close()
$.flash('<%= escape_javascript @notice %>')
