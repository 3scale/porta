<%- @accounts.each do |account| %>
  $("#<%=dom_id(account)%>").find(".plan").text("<%= escape_javascript account.bought_account_plan.name %>");
<%-end-%>
$.colorbox.close()
$.flash('<%= escape_javascript @notice %>')
