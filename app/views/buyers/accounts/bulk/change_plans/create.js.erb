<% accounts.each do |account| %>
  document.querySelector('#<%= dom_id(account) %> td[data-label="Plan"]')
    .innerText = '<%= account.bought_account_plan.name %>'
<% end %>
$.colorbox.close()
$.flash('<%= j @notice %>')
