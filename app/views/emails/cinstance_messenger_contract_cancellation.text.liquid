Dear API Administrator,

We have deleted the application {{application.name}} on application plan {{plan.name}} of your service {{service.name}} in the developer account {{account.name}}.

The API Team.
