Dear User,

{%if provider.multiple_applications_allowed?%}
{{provider.name}} has approved your application {{application.name}} and your API Keys can now be used.
{%else%}
{{provider.name}} has approved your signup for service {{service.name}} ({{plan.name}}) and your API Keys are now active.

If you have problems logging into the account please contact {{ service.support_email }}.
{%endif%}

The {{ provider.name }} API Team
