Dear {{provider.name}},

Application {{application.name}} of your client {{account.name}} is above {{alert.level}}% limit utilization of {{alert.message}}.
Monitor your application usage at {{url}}.

{{ account.name }}
