<% content_for :page_header_title, 'Edit Redirect' %>

<%= semantic_form_for @redirect, :url => provider_admin_cms_redirect_path(@redirect) do |f| %>
  <%= render :partial => 'form', :object => f %>
<% end %>
