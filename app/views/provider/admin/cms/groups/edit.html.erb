<div class="pf-c-content">
  <h1>Edit Group '<%= @group.name %>'</h1>
</div>

<%= cms_form_for(@group) do |f| %>
  <%= render 'form', :f => f %>
  <%= f.actions do %>
    <%= f.commit_button %>
  <% end %>
<% end %>
