<% content_for :page_header_title, "Edit Group '#{@group.name}'"%>

<%= cms_form_for(@group) do |f| %>
  <%= render 'form', :f => f %>
  <%= f.actions do %>
    <%= f.commit_button %>
  <% end %>
<% end %>
