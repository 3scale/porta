<%# If EventTarget is replaceWith'ed immediately, ajax:success and ajax:complete are not called and the spinner will stay %>
setTimeout(() => {
  $('#switch-<%= @switch.setting_name %>-toggle').replaceWith(<%= json render('switch_toggle', switch: @switch) %>);
  $('#switch-<%= @switch.setting_name %>-state').replaceWith(<%= json render('switch_state', switch: @switch) %>);
});
