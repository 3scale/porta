<%= render 'provider/admin/cms/tab_content_title', header: "Layout '#{@page.title}'" %>

<%= cms_new_button_dropdown(CMS::Layout) %>

<%= cms_form_for(@page, :remote => true) do |f| %>
  <div id="cms-template-fields-wrapper">
    <%= render( 'form', :f => f) %>
  </div>
  <%= render '/provider/admin/cms/content_tabs', :f => f %>
<% end %>
