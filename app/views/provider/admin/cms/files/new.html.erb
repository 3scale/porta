<h1>Upload File</h1>

<%= cms_new_button_dropdown(CMS::File) %>

<%= cms_form_for(@file, :html => { :multipart => true }) do |f| %>
  <%= render 'form', :f => f %>

  <%= f.buttons do %>
    <%= f.button 'Create File', button_html: { class: 'pf-c-button pf-m-primary' } %>
  <% end %>
<% end %>
