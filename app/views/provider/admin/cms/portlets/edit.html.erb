<h1>Portlet '<%= @page.title %>'</h1>

<%= cms_new_button_dropdown(CMS::Portlet) %>

<%= cms_form_for(@page, :remote => true) do |f| %>
  <div id="cms-template-fields-wrapper">
    <%= render( 'form', :f => f) %>
  </div>
  <%= render '/provider/admin/cms/content_tabs', :f => f %>
<% end %>
