<div class="pf-c-content">
  <h1>New page</h1>
</div>

<%= cms_new_button_dropdown(CMS::Page) %>

<%= cms_form_for(@page) do |f| %>
  <%= render 'form', :f => f %>
  <%= f.input :draft, :as => :codemirror %>

  <%= f.actions do %>
    <%= f.commit_button 'Create Page' %>
  <% end %>
<% end %>
