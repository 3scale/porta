<%# This file is only loaded by the /p/admin/dashboard/service/:service_id/hits route, which's not called anymore %>
<%# since we've migrated the dashboards to React. %>
<%# Once the leftover dashboard files are removed, we'll be able to remove this one as well. %>
