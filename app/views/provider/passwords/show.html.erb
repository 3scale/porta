<%= javascript_pack_tag 'ChangePassword' %>

<%= tag("div", :id => "pf-login-page-container", :"data-change-password-props" => {:lostPasswordToken => @user.lost_password_token,
  :url => provider_password_path,
  :errors => (@user.errors  || []).map { |key, value| {type: 'error', message: "#{key} #{value}"}}
}.to_json) %>
