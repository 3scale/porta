<% if category.topics.exists? %>
 <ol class="latest_topics">
  <% category.topics.limit(4).each do |topic| %>
   <li>
     <%= link_to h(topic.title), forum_topic_path(topic) %>
   </li>
  <% end %>
 </ol>
<% else %>
  No threads in this category.
<% end %>
