<% if @forum.anonymous_posts_enabled? %>
  <% if logged_in? %>
    <%= form.input :anonymous_user, :as => :boolean, :label => 'Post anonymously?' %>
  <% else %>
    <%= form.input :anonymous_user, :as => :hidden, :input_html => { :value => true } %>
  <% end %>
<% end %>
